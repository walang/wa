machine:
  timezone:
    Asia/Tokyo

dependencies:
  override:
    - curl -sL 'http://prdownloads.sourceforge.net/sbcl/sbcl-1.2.7-x86-linux-binary.tar.bz2' | tar jx
    - cd sbcl-*; sudo INSTALL_ROOT=/usr sh install.sh

test:
  override:
    - ./configure; make test
